<view wx:if="{{visible}}" class="undo-bar" role="status" aria-live="polite">
  <text class="undo-bar__message">{{message}}</text>
  <button class="undo-bar__action" bindtap="handleUndo">撤销</button>
</view>
