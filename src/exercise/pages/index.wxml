<custom-navigation title="运动养宠" background="#ffffff" />

<view wx:if="{{loading && !hasLoaded}}" class="fullscreen-loading">
  <view class="fullscreen-loading__spinner" />
  <view class="fullscreen-loading__text">正在看看它们…</view>
</view>

<view wx:else class="page page--with-navigation">
  <view class="pet-card">
    <view class="pet-stage">
      <view class="bowl-zone">
        <image class="bowl-image" src="{{bowlImage}}" mode="aspectFit" />
        <view class="stage-label">食盆 {{bowlLabel}}</view>
      </view>

      <view class="pets-zone">
        <image class="pet-pair-image" src="{{petImage}}" mode="aspectFit" />
        <view class="stage-label">{{emotionLabel}}</view>
      </view>
    </view>
  </view>

  <view class="balance-card">
    <view class="balance-header">
      <view class="balance-heading">今日任务</view>
      <view class="settings-button" role="button" aria-label="运动设置" bindtap="handleSettings">
        <app-icon name="settings-2" size="24" />
      </view>
    </view>
    <view class="task-list">
      <view class="task-row">
        <text class="task-row__label">日常任务</text>
        <view wx:if="{{todayDailyTaskState === 'pending'}}" class="task-row__remaining">
          <text class="task-row__hint">还需</text><text class="task-row__value">{{todayPendingMinutes}}</text><text class="task-row__unit">分钟</text>
        </view>
        <view wx:elif="{{todayDailyTaskState === 'rest'}}" class="task-row__status">今日休息</view>
        <view wx:else class="task-row__status">已完成</view>
      </view>
      <view class="task-row">
        <text class="task-row__label">加餐任务</text>
        <view class="task-row__end">
          <view wx:if="{{todayExtraTaskState === 'pending'}}" class="task-row__remaining">
            <text class="task-row__hint">还需</text><text class="task-row__value">{{todayExtraPendingMinutes}}</text><text class="task-row__unit">分钟</text>
          </view>
          <view wx:elif="{{todayExtraTaskState === 'completed'}}" class="task-row__status">已完成</view>
          <view wx:else class="task-row__status task-row__status--muted">暂无</view>
        </view>
      </view>
    </view>
    <view wx:if="{{todayOverachievedMinutes > 0}}" class="overachieved">
      今日超额完成 <text>+{{todayOverachievedMinutes}} 分钟</text>
    </view>
  </view>

  <view class="action-section">
    <button
      class="task-button extra-task-button"
      disabled="{{busy}}"
      loading="{{busyAction === 'extra-claim'}}"
      bindtap="handleExtraClaim"
    >
      <view class="extra-task-button__content">
        <text>添加加餐任务</text>
      </view>
    </button>
    <button
      class="task-button task-button--dark"
      disabled="{{busy}}"
      loading="{{busyAction === 'complete'}}"
      bindtap="handleComplete"
    >
      <view class="task-button__title">{{completeButtonText}}</view>
    </button>
    <button
      class="task-button rest-day-button {{restDayDisabled ? 'task-button--disabled' : ''}}"
      disabled="{{restDayDisabled || busy}}"
      loading="{{busyAction === 'rest-day'}}"
      bindtap="handleRestDay"
    >
      <view class="task-button__title">{{restDayButtonText}}</view>
    </button>
  </view>

  <app-dialog
    wx:if="{{restConfirmVisible}}"
    visible="{{true}}"
    title="使用休息日权限"
    content="{{restConfirmContent}}"
    cancel-text="取消"
    confirm-text="确认使用"
    bindcancel="handleRestCancel"
    bindconfirm="handleRestConfirm"
  />

  <app-dialog
    wx:if="{{minutesDialogVisible}}"
    visible="{{true}}"
    title="{{minutesDialogTitle}}"
    custom-body="{{true}}"
    custom-actions="{{true}}"
  >
    <view slot="body" class="minutes-dialog-body">
      <view class="minutes-dialog-input">
        <input
          type="number"
          value="{{minutesInput}}"
          maxlength="5"
          placeholder="{{minutesDialogPlaceholder}}"
          focus
          bindinput="handleMinutesInput"
        />
        <text>分钟</text>
      </view>
    </view>
    <view slot="actions" class="minutes-dialog-actions">
      <button class="minutes-dialog-button" disabled="{{busy}}" bindtap="closeMinutesDialog">取消</button>
      <button class="minutes-dialog-button minutes-dialog-button--confirm" disabled="{{busy}}" bindtap="confirmMinutesDialog">确定</button>
    </view>
  </app-dialog>
</view>
