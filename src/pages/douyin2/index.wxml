<view class="page">
  <view class="template-switch" aria-label="选择图文模板">
    <view
      class="template-switch__item"
      data-template="xiaohongshu"
      bindtap="handleTemplateChange"
    >小红书图文</view>
    <view
      class="template-switch__item template-switch__item--active"
      data-template="douyin2"
      bindtap="handleTemplateChange"
    >抖音短句</view>
  </view>

  <view wx:if="{{!hasCustomContent}}" class="card-swiper card-slide">
    <view class="card-item card-item--loading">
      <view class="card-loading">
        <view class="card-loading__title">当前是默认文案</view>
        <view class="card-loading__desc">请编辑文案</view>
      </view>
    </view>
  </view>

  <swiper
    wx:else
    class="card-swiper"
    current="{{activeIndex}}"
    indicator-dots="{{renderedImageUrls.length > 1}}"
    indicator-color="rgba(0, 0, 0, 0.18)"
    indicator-active-color="#111111"
    bindchange="handleSwiperChange"
  >
    <swiper-item wx:for="{{renderedImageUrls}}" wx:key="*this" class="card-slide">
      <view class="card-item">
        <image class="card-image" src="{{item}}" mode="widthFix" show-menu-by-longpress />
      </view>
    </swiper-item>

    <swiper-item wx:if="{{!renderedImageUrls.length}}" class="card-slide">
      <view class="card-item card-item--loading">
        <view class="card-loading">
          <view class="card-loading__title">卡片生成中</view>
          <view class="card-loading__desc">正在同步页面和导出样式</view>
        </view>
      </view>
    </swiper-item>
  </swiper>

  <view class="page-count" wx:if="{{pages.length}}">
    {{activeIndex + 1}} / {{pages.length}}
  </view>

  <text-card-action-bar
    disabled="{{isGenerating}}"
    label="抖音短句操作"
    bind:action="handleAction"
  />

  <canvas
    type="2d"
    id="douyin2ExportCanvas"
    class="export-canvas"
    style="width: 1px; height: 1px;"
  />
</view>
